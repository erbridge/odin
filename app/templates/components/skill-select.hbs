{{#each model as |skill|}}

  <div class='skill'>

    {{#if editable}}

      {{#link-to 'model.edit' skill}}{{skill.name}}{{/link-to}}

    {{else}}

      {{skill.name}}

    {{/if}}

  </div>

{{/each}}

{{#if editable}}

  <div class='skill'><button {{action 'createSkill'}}>New Skill</button></div>

{{/if}}
